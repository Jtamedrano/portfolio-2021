import React from 'react';

interface Props {}

const ProjectView = (props: Props) => {
  return (
    <section id="projects">
      <h2>Projects</h2>
    </section>
  );
};

export default ProjectView;
